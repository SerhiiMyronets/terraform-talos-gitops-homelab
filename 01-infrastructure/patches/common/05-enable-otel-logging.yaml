machine:
  logging:
    destinations:
      - endpoint: "tcp://otel-gateway-collector.observability.svc:6050"
        format: "json_lines"
        extraTags:
          service_name: logs
          service_namespace: talos