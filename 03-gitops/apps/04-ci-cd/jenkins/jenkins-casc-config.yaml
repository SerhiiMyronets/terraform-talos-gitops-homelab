apiVersion: v1
kind: ConfigMap
metadata:
  name: jenkins-jcasc-config
  namespace: jenkins
  labels:
    jenkins-jenkins-config: "true"
data:
  jcasc.yaml: |
    jenkins:
      numExecutors: 0
      mode: NORMAL
      markupFormatter:
        plainText: {}
          
      systemMessage: "🔧 Jenkins configured via JCasC"
              
          
      clouds:
        - kubernetes:
            name: "kubernetes"
            serverUrl: "https://kubernetes.default"
            jenkinsUrl: "http://jenkins.jenkins.svc.cluster.local:8080"
            jenkinsTunnel: "jenkins-agent.jenkins.svc.cluster.local:50000"
            containerCapStr: "10"
            namespace: "jenkins"
    
    credentials:
      system:
        domainCredentials:
          - credentials:
              - string:
                  scope: GLOBAL
                  id: sonar-token
                  description: "SonarQube Token"
                  secret: ${SONAR_TOKEN}
              - usernamePassword:
                  scope: GLOBAL
                  id: github-access-token
                  description: "Github Access Token"
                  username: ${GITHUB_ACCESS_USERNAME}
                  password: ${GITHUB_ACCESS_TOKEN}
              - usernamePassword:
                  description: "lolo"
                  id: "lolo"
                  password: "{AQAAABAAAAAw/w9odRUbSHCrllUpX/pLNY4/FQmS4XsyQ1poxSRvHrLqkFPs/NXMZjfPSAkF3O8QVgdl4g0rvyj9aZVqQ6s3Qg==}"
                  scope: GLOBAL
                  username: "SerhiiMyronets"
                    
    unclassified:
      location:
        url: https://jenkins.cluster
      sonarGlobalConfiguration:
        buildWrapperEnabled: true
        installations:
        - credentialsId: "sonar-token"
          name: "SonarQube"
          serverUrl: "http://sonarqube-sonarqube.sonarqube.svc.cluster.local:9000"
      globalLibraries:
        libraries:
          - defaultVersion: "main"
            name: "jenkins-shared-lib"
            retriever:
              modernSCM:
                scm:
                  git:
                    id: jenkins-shared-lib
                    credentialsId: "github-access-token"
                    remote: "https://github.com/DevSecOps-homelab/jenkins-shared-lib"
                    traits:
                      - "gitBranchDiscovery"
    
    
    security:
      apiToken:
        creationOfLegacyTokenEnabled: false
        tokenGenerationOnCreationEnabled: false
        usageStatisticsEnabled: true
    
    appearance:
      prism:
        theme: PRISM
      themeManager:
        disableUserThemes: false
        theme: "darkSystem"
    
#    jobs:
#      - script: >
#          pipelineJob('seed-job') {
#            definition {
#              cps {
#                script('''
#                  multibranchPipelineJob('currencyservice') {
#                    branchSources {
#                      github {
#                        id('currencyservice-github')
#                        repoOwner('DevSecOps-homelab')
#                        repository('currencyservice')
#                        credentialsId('github-access-token')
#                      }
#                    }
#                    orphanedItemStrategy {
#                      discardOldItems {
#                        numToKeep(10)
#                      }
#                    }
#                    triggers {
#                      periodic(1)
#                    }
#                  }
#                ''')
#                sandbox(true)
#              }
#            }
#          }