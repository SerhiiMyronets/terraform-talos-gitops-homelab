#apiVersion: networking.k8s.io/v1
#kind: Ingress
#metadata:
#  name: jaeger-ui
#  namespace: monitoring
#  annotations:
#    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
#    cert-manager.io/cluster-issuer: ingress
#spec:
#  ingressClassName: nginx
#  rules:
#    - host: jaeger.cluster
#      http:
#        paths:
#          - path: /
#            pathType: Prefix
#            backend:
#              service:
#                name: jaeger-query
#                port:
#                  number: 16686
#  tls:
#    - hosts:
#        - jaeger.cluster
#      secretName: jaeger-ui-tls
#---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jaeger-public-ui
  namespace: monitoring
  annotations:
    nginx.ingress.kubernetes.io/auth-url: "https://oauth.serhii.link/oauth2/auth"
    nginx.ingress.kubernetes.io/auth-signin: "https://oauth.serhii.link/oauth2/start?rd=https://$host$request_uri"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  rules:
    - host: jaeger.serhii.link
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jaeger-query
                port:
                  number: 16686