apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-dashboard-config
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  kafka-dash.json: |
    {
      "gnetId": 24626,
      "datasource": "VictoriaMetrics"
    }